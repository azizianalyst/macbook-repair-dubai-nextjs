import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/imac-overheating-fix-dubai");
export { default } from "@/views/IMacOverheatingFix";
