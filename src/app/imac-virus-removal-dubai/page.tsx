import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/imac-virus-removal-dubai");
export { default } from "@/views/IMacVirusRemoval";
