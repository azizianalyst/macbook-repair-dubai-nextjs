import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/how-to-recover-macbook-password");
export { default } from "@/views/GuideRecoverMacBookPassword";
