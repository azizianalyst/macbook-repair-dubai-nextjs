import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/macbook-backup-service-dubai");
export { default } from "@/views/MacBookBackupService";
