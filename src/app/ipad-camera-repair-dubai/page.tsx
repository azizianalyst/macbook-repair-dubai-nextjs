import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/ipad-camera-repair-dubai");
export { default } from "@/views/IPadCameraRepair";
