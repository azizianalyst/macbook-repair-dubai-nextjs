import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/ipad-full-diagnostic-dubai");
export { default } from "@/views/IPadFullDiagnostic";
