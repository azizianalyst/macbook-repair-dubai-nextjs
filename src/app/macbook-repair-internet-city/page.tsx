import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/macbook-repair-internet-city");
export { default } from "@/views/MacBookRepairInternetCity";
