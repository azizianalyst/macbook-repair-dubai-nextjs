import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/how-to-change-macbook-name");
export { default } from "@/views/GuideChangeMacBookName";
