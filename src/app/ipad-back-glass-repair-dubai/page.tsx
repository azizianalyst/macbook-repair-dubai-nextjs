import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/ipad-back-glass-repair-dubai");
export { default } from "@/views/IPadBackGlassRepair";
