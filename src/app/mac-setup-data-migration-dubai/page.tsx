import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/mac-setup-data-migration-dubai");
export { default } from "@/views/MacSetupConfiguration";
