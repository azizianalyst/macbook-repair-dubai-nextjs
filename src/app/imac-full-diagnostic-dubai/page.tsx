import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/imac-full-diagnostic-dubai");
export { default } from "@/views/IMacFullDiagnostic";
