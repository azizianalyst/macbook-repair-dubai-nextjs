import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/ipad-icloud-unlock-dubai");
export { default } from "@/views/IPadIcloudUnlock";
