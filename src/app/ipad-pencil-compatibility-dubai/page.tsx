import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/ipad-pencil-compatibility-dubai");
export { default } from "@/views/IPadPencilCompatibility";
