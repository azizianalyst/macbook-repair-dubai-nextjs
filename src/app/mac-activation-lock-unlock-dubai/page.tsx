import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/mac-activation-lock-unlock-dubai");
export { default } from "@/views/MacActivationLockUnlock";
