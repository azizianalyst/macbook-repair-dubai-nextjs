import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/how-to-clean-a-macbook-screen");
export { default } from "@/views/GuideCleanMacBookScreen";
