import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/blog/macbook-back-to-school-dubai");
export { default } from "@/views/BlogMacBookBackToSchool";
