import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/macbook-repair-umm-al-quwain");
export { default } from "@/views/MacBookRepairUmmAlQuwain";
