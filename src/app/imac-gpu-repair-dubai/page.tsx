import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/imac-gpu-repair-dubai");
export { default } from "@/views/IMacGpuRepair";
