import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/applecare-macbook-repair-dubai");
export { default } from "@/views/AppleCareMacBookRepair";
