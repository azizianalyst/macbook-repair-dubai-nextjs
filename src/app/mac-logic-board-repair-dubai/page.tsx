import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/mac-logic-board-repair-dubai");
export { default } from "@/views/MacLogicBoardRepair";
