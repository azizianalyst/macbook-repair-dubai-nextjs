import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/sell-macbook-dubai");
export { default } from "@/views/SellMacBookDubai";
