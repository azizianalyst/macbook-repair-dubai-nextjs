import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/apple-service-center-dubai");
export { default } from "@/views/AppleServiceCenter";
