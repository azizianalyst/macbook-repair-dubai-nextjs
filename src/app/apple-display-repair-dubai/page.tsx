import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/apple-display-repair-dubai");
export { default } from "@/views/AppleDisplayRepair";
