import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/how-to-clean-macbook-pro-keyboard");
export { default } from "@/views/GuideCleanMacBookKeyboard";
