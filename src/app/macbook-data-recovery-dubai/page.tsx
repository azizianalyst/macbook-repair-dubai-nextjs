import type { Metadata } from "next";
import { metaForPath } from "@/lib/page-meta";

export const metadata: Metadata = metaForPath("/macbook-data-recovery-dubai");
export { default } from "@/views/MacBookDataRecovery";
